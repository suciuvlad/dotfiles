> [!danger] Delete more than you add
> Notes are liability, not asset. But never delete blind, and never delete the loop.

**Orchestrates:** `/wiki-lint` (orphans), then an archive-only prune.
**Cut:** delete more than you add.

## Prune safety (mandatory)
- **Never delete directly.** First write a dry-run manifest at `_archive/prune/{{date}}/manifest.md` listing every candidate and the reason.
- Move candidates into `_archive/prune/{{date}}/` only **after explicit user approval**. Archive, do not erase.
- **Never prune** `00`–`09`, `loop/`, `scripts/`, `Loop.canvas`, `log.md`, or anything outside `topics/{{slug}}/`.

## Pruned (after approval)
- 

## Runs
<!-- newest first -->

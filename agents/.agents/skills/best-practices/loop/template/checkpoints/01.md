> [!note] Read first
> Sources you don't understand, you cannot use. Open the call sites, the consumers, the prior art.

**Orchestrates:** `/autoresearch "{{goal}}"` (broad/strategic) or `/research --deep "{{goal}}"` (version numbers, named APIs, recent events).
**Cut:** read before write.

## Do
- Gather at least 3 independent sources on {{goal}}. Capture raw; do not interpret yet.
- File each into `topics/{{slug}}/sources/` using the template below.
- If you cannot find a real source, say so. **Never invent a source, URL, or quote.**

## Source artifact template
    title:
    url:
    retrieved_at: YYYY-MM-DD
    via_command: /autoresearch | /research --deep
    quoted_evidence: "verbatim quote supporting any claim drawn from this source"

## Feeds
- [[topics/{{slug}}/sources]]

## Runs
<!-- newest first: YYYY-MM-DD — N sources → [[topics/{{slug}}/sources/...]] -->
